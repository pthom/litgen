[mypy]
exclude = (setup.py|_skbuild|pybind11|^build/|examples_real_libs|litgenold|^venv|^example)

# Explicitly blacklist modules in use
# that don't have type stubs.
;[mypy-pytest.*]
;ignore_missing_imports = True
